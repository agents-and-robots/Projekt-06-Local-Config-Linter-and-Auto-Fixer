# Test: Agent

## Natürliche Sprache
Der Agent soll original_config und fixed_config zurückgeben (Baseline: leer).

## Maschinenlesbare Struktur
input:
  lines:
    - "HOST=localhost"

expected:
  original_config: {}
  fixed_config: {}
